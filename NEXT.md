# PLAN.md — España Transparente: Product Direction

Generated by /office-hours on 2026-05-22. Last updated 2026-05-25 (evening — BORME/CNMC bulk runs).
Branch: dev
Repo: espana-transparente
Status: IN PROGRESS — data coverage complete through Phase C. **Next major phase: Phase D — Information Architecture redesign** (see section below). Data-coverage status preserved further down.

---

## Vision

> "I show you the relevant information the government does not want you to know.
> I help you understand what IPC, inflation, and public debt actually mean for your life."

España Transparente is two things simultaneously: an accountability tool for researchers and journalists,
and an economic literacy platform for citizens who never studied economics. Nobody else has built this
combination. The official transparency portal (`transparencia.gob.es`) is a compliance instrument, not
a product. The INE publishes numbers nobody can read. This project bridges both gaps.

---

## Problem Statement

Public money in Spain is traceable in theory and opaque in practice. Contracts, subsidies, EU funds,
asset declarations, and institutional appointments are scattered across incompatible official sources.
Meanwhile, the economic indicators that affect every citizen's daily life (IPC, salario medio, deuda
pública) are published as raw tables that require economics training to interpret.

---

## What Makes This Compelling

- The only Spanish portal that connects money → officials → organizations in one searchable place.
- The only place that tells your dad what IPC actually means for his rent and salary.
- All from official public sources — no editorializing, no partisan framing.

---

## Constraints

- Supabase Free (500 MB). No new heavy tables without auditing current usage first.
- `transparencia.gob.es` is the government's own portal — Spain has a documented pattern of
  labeling accountability journalism as partisan. Editorial neutrality is non-negotiable.
- Congress portal rate-limits at 1.5s/request. Declaration PDF parsing must be async and resumable.
- Vercel Hobby deployment. No server-side heavy compute.

---

## Phase D — Information Architecture redesign (ACTIVE NEXT PHASE)

Decided 2026-05-25, approved. This is the next major body of work after the data-coverage
phases (A–C). It is pure IA/UI — **no new data pipeline needed**. No time pressure: the
correct redesign was chosen over the fast patch.

### The problem

Data coverage has outrun the interface. ~20 datasets now exist, but the IA is organized
around the data model (17 source-named sections in 3 groups: Personas / Dinero y leyes /
Conexiones y contexto) instead of around what a visitor wants to know. A first-time visitor
cannot find what they came for. The richest asset — the EntityTrail graph (money → person →
org → case) — is buried inside detail pages, invisible from the front door. The fix is an
**intent layer on top of the source data**, not a reskin.

### Audience priority (locked)

1. **Citizen, "what affects me"** (theme-guided, no name in mind) — *most important*.
2. **Citizen, named-entity lookup** (arrives with a deputy / party / scandal) — almost equal.
3. **Researcher, follow-the-thread** — must be decently covered.

The front door leads with #1; the design serves all three by layer.

### Front door (locked)

Scrollytelling "state of the nation": a curated top-to-bottom narrative that teaches as you
scroll. Each anchor is a real number with plain-language meaning (deuda per cápita · qué es;
IPC del mes · qué significa para ti; el mayor contrato del mes). Editorial in *sequencing*,
strictly factual in *selection*.

### Chosen approach: threads as the spine

Reorganize the whole IA around ~5 durable citizen threads. The scrollytelling home crowns
them; each thread is a curated narrative-plus-explore landing weaving several datasets;
source pages become the deep layer reachable *within* a thread; EntityTrail is the
connective tissue. Top-level nav: 17 items → ~5 threads + persistent search.

| Thread | Citizen question | Primary datasets (deep layer) |
|---|---|---|
| **Dinero** | ¿A dónde va el dinero público? | trazabilidad, presupuestos, contratos, subvenciones, fondos UE, organizaciones (contrapartes) |
| **Economía** | ¿Cómo me afecta la economía? | indicadores (IPC, deuda, paro, salario, PIB) + explicaciones en lenguaje llano |
| **Integridad** | ¿Es honesto quien me representa? | declaraciones, puertas giratorias, procesos judiciales, nombramientos institucionales |
| **Poder** | ¿Quién manda y cómo decide? | gobierno, partidos, diputados, senado, votaciones, iniciativas |
| **Tu territorio** | ¿Qué pasa donde vivo? | gasto autonómico (CCAA), gasto municipal, tus representantes por provincia |

**"Personas" stops being a top-level group.** People and organizations become connective
*entities* that appear as actors inside every thread, reached primarily via global search
and via the threads where they act.

### Premises (agreed)

1. The redesign ADDS an intent layer; it does NOT delete source sections. Every source page
   survives as the deep layer. Richness preserved; only the entry changes.
2. **Curation by mechanical rule, never opinion.** Each anchor's selection rule is factual
   and sourced. The narrative teaches *what a number means*, never *asserts a scandal*.
   **Vote divergence is NOT a front-door anchor — deep-page / analytical only** (see fix
   note below and AGENTS.md §2).
3. **Named-entity lookup is a fixture, not a section:** persistent, instant global search
   from every screen.
4. **EntityTrail becomes both an embedded component AND a standalone surface** (out of the
   detail-page basement), but stays tier 3 — not the front door. Reuses existing trail
   queries; needs a non-graph (list/table) fallback for accessibility + small screens.
5. **Future data scope is governed by "does it complete a story we already tell," not "is it
   available."** New data must deepen an existing thread, not add a 21st directory item.
   *This supersedes "ingest what's available" as the scoping rule for all future data work
   (see the Data Gaps Register below — re-read it through this lens).*

### Divergence-on-homepage: root cause fixed (2026-05-25)

Agents repeatedly tried to put vote divergence on the homepage. Drivers: AGENTS.md §2
instructed "build UI to highlight divergences"; CLAUDE.md flagged `get_divergences()` as the
only named "Key function"; DESIGN.md reserves the signal color exclusively for divergences;
the live homepage renders a divergence section. **Patched:** AGENTS.md §2 (scoped to
deep-page/data-modeling, "No pongas recuentos de divergencias en la home") + CLAUDE.md note.
**Build status:** fixed 2026-05-25. `DESIGN.md` now scopes signal color to highlighted
evidence / sourced anomalies / exception counts, and the homepage divergence section was
removed from `web/src/app/page.tsx`.

### Phase D foundation slice (started 2026-05-25)

- `web/src/lib/thread-config.ts` is now the source of truth for the five intent threads:
  Dinero, Economía, Integridad, Poder and Territorio.
- Top-level navigation is generated from the thread config. Existing source routes stay
  stable and appear as deep links inside their thread menus.
- New thread routes exist for `/dinero`, `/economia`, `/integridad`, `/poder` and
  `/territorio`.
- `/dinero` and `/economia` have first-pass live-data anchor cards using existing cached
  data helpers. The other three thread routes are factual source indexes ready for the next
  composition pass.
- Sitemap includes all five thread routes.

### Execution notes (direction for the eng review, not final architecture)

- **Routing:** keep all existing source paths stable (`/diputados/[id]`, `/contratos/[id]`…)
  — no redirects, no SEO loss. Threads are *new* top-level routes (`/dinero`, `/economia`,
  `/integridad`, `/poder`, `/territorio`) that compose existing data and link down. Source
  pages re-parented *conceptually* (breadcrumbs / thread context), not by moving URLs.
- **Search:** reuse the existing `common.search_refresh` / Postgres FTS table — no new infra.
  Confirm it indexes every entity type the threads expose. "Instant" = client debounce over
  the existing endpoint, within Supabase Free.
- **Home anchors:** built on the existing `web/src/lib/data/` cached boundary + ISR, never
  live per-request queries. Each anchor needs an empty/stale fallback (e.g. "mayor contrato
  del mes" → widen to "del trimestre"; stale IPC → last-known + dated source label). The home
  must always look complete.
- **Mobile + motion:** mobile-first calm sections; honor `prefers-reduced-motion`; the
  ~5-thread nav collapses to a drawer / bottom bar with search prominent.
- **Locale:** single-locale (Spanish). No i18n layer.

### Open questions (resolve in eng review + hallway test)

1. **Dual-home datasets** — proposed defaults: organizaciones → connective entity;
   instituciones → Integridad (or Poder); CCAA/municipios → Territorio (not Dinero);
   iniciativas → Poder.
2. **4 or 5 threads** — is "Tu territorio" a peer thread or a cross-cutting filter over the
   other four?
3. **Scrollytelling depth** — lean "calm sectioned" (lighter, accessible, fits Vercel Hobby)
   over full scroll-driven motion.
4. **`/distorsion`** (deferred) placement when revived — Poder or Territorio.
5. **DESIGN.md signal-color broadening** — needs explicit approval.

### Success criteria

- No-name visitor reaches a relevant, understood answer in one scroll + one click (vs.
  parsing a 17-item directory today).
- Named visitor reaches that entity from any screen via search in one action.
- Every source page stays reachable; zero orphaned datasets; nav top level 17 → ~5 + search.
- A written mechanical curation rule exists for every home anchor (neutrality defensible).
- A one-line scoping test exists for any new dataset ("which thread does it deepen?").
- No divergence surface anywhere on the home.

### Next validation action

**5-person hallway test of the thread vocabulary:** show five non-technical people the five
thread questions and, for something each genuinely wants to know, ask which thread they'd
click. Mismatches = taxonomy corrections. Then run an engineering review to lock taxonomy,
routing, and the 4-vs-5 decision.

---

## Current Status (2026-05-24)

The project has advanced faster than the original phased plan. Here's where everything stands:

### ✅ Phase A — Economic Literacy (COMPLETE)

- `web/src/lib/indicator-explanations.ts` with typed `IndicatorExplanation` interface
- Plain-language explanations for IPC, IPC_VAR_MENSUAL, IPC_VAR_ANUAL
- One-liner subtitles on index cards, long explanations + "¿Qué significa esto para ti?" on detail pages
- `CopyLinkButton` component for shareable per-indicator deep links
- `.limit(500)` on `getIndicators()`
- `content:audit` CI rule: every indicator_code must have an explanation entry
- vitest tests for explanations + copy-link behavior
- Only 3 IPC indicators exist in the DB (salario_medio, paro, deuda, PIB require new ETL pipelines — filed as gaps)

### ✅ Phase B — Data Closure: Declarations + SEPI (COMPLETE)

- 566 structured declaration records for 349 politicians (via OCR + OpenData pipelines)
- 12 SEPI subsidiaries with board data surfaced on `/organizaciones/[id]`
- OCR pipeline (`declaraciones_ocr.py`) handles `bienes_rentas` and `intereses_economicos` PDFs
- OpenData pipeline (`opendata_intereses.py`) ingests machine-readable docacteco JSON
- 182 ETL tests pass
- CI workflow runs OCR (50 PDFs/week) + OpenData + search refresh
- **Remaining OCR**: 67/418 bienes_rentas PDFs processed (continues weekly in CI, 50 per batch)

### ✅ Phase B.5 — Judicial Accountability (COMPLETE)

- `etl/src/judicial/wikipedia.py` — scrapes the Wikipedia annex of Spanish corruption cases
  - 160 named cases with procedural status, territory, organizations, crimes
  - 96 organization actors extracted, reviewed, and party-matched (63 linked to `parties` table)
  - Individual person extraction from case pages via `--extract-people` flag (wiki page parsing for "Imputados"/"Acusados" sections)
  - Matching now covers both `organizations` AND `parties` tables
- `etl/src/judicial/cgpj.py` — ingests CGPJ aggregate statistics
- `etl/src/judicial/review.py` — review gate for approving/rejecting actor links
- `corruption_case_actors` now has `party_id` FK → entity_trail connects judicial cases to politicians via party membership
- Added `'wikipedia'` to allowed `corruption_cases.source_type` values
- Wikipedia scraper + CGPJ scraper added to weekly CI (with `--extract-people`)

### ✅ Phase C v1/v2/v3 — Unified Entity Graph (COMPLETE)

- `v_entity_summary` materialized view with organization + person totals
- `get_entity_trail()` SQL function: returns connected people, orgs, AND judicial cases for any entity
  - Connections: SEPI appointments, revolving doors, BORME directorships, government positions, power relationships, contract/subsidy counterparties, **judicial cases** (via party membership), **CNMC lobbying groups** (for organizations)
  - BORME officers now also appear in organization entity trails (people working at this company)
  - Judicial cases appear as "Casos judiciales" group in EntityTrail UI
- `EntityTrail` React component: renders cross-entity connections with 3 groups (Personas, Organizaciones, Casos judiciales)
- Integration: added to `/organizaciones/[id]` and `/diputados/[id]` with Suspense loading
- Search: entity trail data available

**Remaining**: BORME and CNMC pipelines need more data (currently 1 and 10 rows). Surface will auto-populate as pipelines accumulate data in weekly CI.

### 🚧 BORME Company Directors (IN PROGRESS — bulk run started 2026-05-25)

- Schema + ETL (`src/borme/officers.py`) + UI done
- **209 officers across 17 orgs** (up from 9 officers / 2 orgs after manual bulk run)
- ~722 companies + ~2,200 public bodies remaining to process
- OpenMercantil free tier: 200 req/day — bulk runs limited by API quota
- Improvements added 2026-05-25: municipal position filtering, match validation, better token matching
- Fuzzy matching to politicians implemented (0.85 confidence threshold, 0 matches so far)
- Weekly CI processes 200 orgs/batch; bulk runs accelerate when API quota allows
- **Remaining**: Continue ingestion until coverage is useful; surface matches on politician pages

### 🚧 CNMC Lobbying Register (IN PROGRESS — bulk run completed 2026-05-25)

- Schema + scraper (`src/lobbying/rgi.py`) done; all 618 groups scraped
- **33 organization links** (up from 10 after fixed GIN-index query)
- 613 groups remain unlinked to orgs
- GIN trigram index used for efficient fuzzy matching
- **Remaining**: Surface reviewed lobbying links on organization pages

---

## Data Gaps Register

| Data Source | Current Status | Priority | Notes |
|---|---|---|---|
| Asset/income declarations | ✅ Complete | Done | OCR + OpenData pipelines shipping weekly |
| SEPI subsidiary boards | ✅ Complete | Done | 12 subsidiaries with board data |
| BORME company directors | 🚧 In progress | Now | Ingesting 200/week; EntityTrail surfaces org→people connections |
| Corruption proceedings + contract links | ✅ Complete | Done | 160 cases + 96 reviewed actors (63 party-matched); EntityTrail connected; person extraction live |
| Lobbying register (CNMC RGI) | 🚧 In progress | Now | Schema + scraper done; EntityTrail surfaces org→lobbying connections |
| Economic indicators (salario, paro, PIB) | ✅ Complete | Done | `indicadores_ampliados.py`: PIB, PIB_VAR_ANUAL, TASA_PARO, PARADOS, SALARIO_MEDIO (379 rows); explanations in place |
| Deuda pública | ✅ Complete | Done | `bde.py` via Eurostat: 31 years of data; explanation added |
| Ministerial meeting agendas | Not started | Later | Partially on SAGE portal |
| Historical budget execution | Not started | Later | Intervención General publishes monthly reports |
| Lobbying meetings (SAGE) | Not started | Later | Ministerial agenda disclosures |

---

## What To Do Now (updated 2026-05-25)

**THE priority is now Phase D — Information Architecture redesign (see section above).**
The data-coverage items below are ongoing / background (CI-driven) and not blocking Phase D.

### ✅ DONE: Judicial entity graph connection (Steps 1-4)

- `corruption_case_actors` now has `party_id` → 63/96 actors matched to political parties
- `get_entity_trail()` includes judicial cases (via party membership), CNMC lobbying, and BORME officers on org pages
- EntityTrail UI shows "Casos judiciales" group
- Wikipedia scraper: party matching + `--extract-people` person extraction from case pages
- Migration: `20260625000000_judicial_entity_graph.sql` applied

### ✅ DONE: Economic indicators (Step 5)

- `etl/src/ine/indicadores_ampliados.py`: PIB, PIB_VAR_ANUAL, TASA_PARO, PARADOS, SALARIO_MEDIO
- 379 data points ingested across 5 indicators
- Explanations in `indicator-explanations.ts` for all 8 indicator codes
- `content:audit` now checks all 3 ETL files (indicadores.py, indicadores_ampliados.py, bde.py)
- CI updated: `indicadores_ampliados` weekly, `wikipedia --extract-people` weekly
- `db.py` fixed: Supabase pooler read-write session override

### 1. Continue BORME bulk ingestion (rate-limited)

Bulk run 2026-05-25 increased BORME from 9 to 209 officers (17 orgs).
OpenMercantil free tier caps at 200 req/day — run `--limit 200 --resume` daily
until the ~722 remaining companies are processed. Public bodies (2,200)
are lower priority since most won't have Mercantil Registry entries.

CNMC: all 618 groups scraped, 33 linked to orgs. Run `--resume` periodically
to refresh links as new orgs enter the DB.

### 2. OCR batch completion

351 PDFs remaining at 50/week in weekly CI. Not blocking anything.

### 3. Surface judicial data on party pages — now subsumed into Phase D

Now that corruption actors are linked to parties (63/96), the `/partidos/[id]` page could
show which cases involve each party. **This is no longer a standalone task — it belongs in
the Integridad / Poder threads of the Phase D redesign** and should be designed there, not
bolted onto the current `/partidos` page in isolation.

---

## Open Questions

1. **Declaration PDF format diversity:** ✅ Resolved — OCR pipeline (`easyocr`) handles format diversity; OpenData pipeline covers machine-readable cases.
2. **Supabase Free storage:** ✅ Audited during Phase B. OCR stores extracted text (not PDFs). Within limits.
3. **Economic explanations authorship:** Reviewed by GPT-5.5. `content:audit` CI check enforces neutrality. No further review process defined.
4. **Phase C performance:** ✅ Resolved — `v_entity_summary` materialized view implemented. Cold starts handled with loading skeletons.

---

## CI Status Notes

- **Web CI** (`lint`, `ui:audit`, `content:audit`, `search:routes`, `build`): ✅ Passing on push/PR to main
- **ETL check** (`pytest`, `--dry-run`): ✅ Passing on push/PR to main
- **ETL daily** (diputados, asistencia, INE, contratos, BDNS, fotos, search): Runs 4am UTC — depends on `DATABASE_URL` secret
- **ETL weekly** (cods, declaraciones, iniciativas, gobierno, responsables, presupuestos, UE, senado, OCR, BORME, lobbying, OpenData, search): Runs Mondays 5am UTC — depends on `DATABASE_URL` secret
- `DATABASE_URL` secret updated 2026-05-24 to match current Supabase password

---

## GSTACK REVIEW REPORT (historical — 2026-05-22)

All 6 decisions (D1-D6) implemented. Architecture findings addressed. Test coverage increased.
Performance fixes applied. See git history for details.

<!--
Original plan sequence: A → B → C.
Actual execution: A, B, B.5 foundation, C v1/v2 all shipped in parallel.
Remaining: B.5 population, C UI slice, BORME/lobbying surface, OCR completion.
-->
